# Legacy static landing artifacts

Los archivos `index.html`, `index_legacy.html`, `index_tailwind.html`, `styles.css`, `app.js`, `translations.js`, `landing_translations.js`, `assets/` y `dist/` corresponden al prototipo previo basado en HTML estático.

Se mantienen temporalmente para referencia histórica. Una vez validado el despliegue en Next.js pueden eliminarse del repositorio para reducir ruido.
